CORREÇÃO DOS ERROS DO CONSOLE

- Corrigido o erro fatal:
  buildSavedDiverseLiveBattlePairs is not defined

- O service worker foi tornado seguro:
  se uma API externa falhar por CORS/403, devolve JSON vazio válido
  e a app passa para a próxima fonte.

- SofaScore direto foi desativado sem proxy para evitar 403 no browser.
  Continua disponível se configurares SOFASCORE_PROXY_URL.

- TheSportsDB eventsday foi evitado em localhost quando bloqueia CORS.

- O aviso beforeinstallprompt não é erro crítico.
  Apenas significa que o banner PWA não foi mostrado automaticamente.

CORREÇÃO CONSERVADORA

Esta versão foi feita a partir da pasta original que enviaste, não da versão incompleta anterior.

Corrigido:
1) Não mexi no formulário/design inicial.
2) Mantive/restaurei updateSummary porque no original existe.
3) Antes de escrever em worldcupextraMatches, lê o documento existente e verifica kickoff.
   Se kickoff ainda estiver no futuro, NÃO grava nada nesse jogo.
4) football-data.org é ignorada quando abres em http://127.0.0.1 porque essa API só aceita CORS para http://localhost.
   Para testar football-data usa http://localhost:5502, não 127.0.0.1.
5) SofaScore já não calcula minuto por timestamp. Só aceita minuto direto se a fonte o trouxer.
6) sw.js corrigido para não criar Response 204 com body.

IMPORTANTE:
Depois de substituir, abre DevTools > Application > Service Workers > Unregister, e depois Ctrl+F5.

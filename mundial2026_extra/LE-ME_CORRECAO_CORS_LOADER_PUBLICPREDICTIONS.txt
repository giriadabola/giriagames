CORREÇÕES DESTA VERSÃO

1) Voltei a colocar a parte final do script.js que tinha desaparecido:
   - init()
   - initFirebase()
   - loadPublicPredictions()
   - listener em tempo real de worldcupextraMatches
   - loader inicial com segundos

2) Mantive a tua regra do kickoff:
   Se worldcupextraMatches/match_XXX tiver kickoff no futuro, a app NÃO grava nada nesse documento.
   Isto impede o próximo jogo de ser preenchido como 0-0 antes de começar.

3) Corrigi CORS:
   - football-data.org é saltado automaticamente quando abres em http://127.0.0.1, porque essa API costuma aceitar http://localhost mas não 127.0.0.1.
   - abre com http://localhost:5502 em vez de http://127.0.0.1:5502 se quiseres testar football-data no Live Server.
   - removi chamadas automáticas a api.allorigins.win e corsproxy.io, porque estavam a encher o console de erros.
   - SofaScore/Yallakora/365Scores só tentam carregar se configurares proxy próprio.

4) Corrigi sw.js:
   - já não cria Response com body em status 204.

5) Corrigi fallback de fase2.js e battle-system.js:
   - se loadPublicPredictions ainda não existir por algum atraso/cache, não rebenta o JavaScript.

IMPORTANTE:
Depois de substituir ficheiros, faz hard refresh:
- Ctrl + F5
- ou DevTools > Application > Service Workers > Unregister
- e limpa cache do site se continuar a aparecer sw.js?v=20260611pwa6.

APIS NÃO BLOQUEIAM CARREGAMENTO

Pelos logs, o maior atraso vinha de chamadas externas com CORS/erro, especialmente worldcup26.ir em localhost.

Alterações:
- worldcup26.ir deixa de ser chamado em localhost para evitar CORS e atrasos.
- APIs lentas têm timeout curto.
- O painel principal já não espera por APIs lentas.
- A aba Jogos aparece primeiro.
- Minutos/resultado live mostram uma rodinha só nessa zona até atualizar.
- Resultados/Battles/Tabela mostram rodinha própria se ainda estiverem a carregar.
- fase2.js deixou de forçar carregamento total no arranque.
- APIs que falham passam a fallback/cache/local sem bloquear a interface.

O aviso beforeinstallprompt continua a não ser erro crítico.
